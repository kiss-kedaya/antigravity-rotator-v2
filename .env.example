# Antigravity Rotator V2 - Environment Variables

# Google OAuth Credentials (Defaults are provided in code, override if needed)
# GOOGLE_CLIENT_ID=your_client_id
# GOOGLE_CLIENT_SECRET=your_client_secret

# Network Proxy (Optional, defaults to http://127.0.0.1:7890)
# PROXY=http://127.0.0.1:7890

# OpenClaw Binary Path (Optional, defaults to 'openclaw' in PATH)
# OPENCLAW_BIN=openclaw
